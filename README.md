# Logistic-Regression-Model
This project covers EDA, preprocessing, building and training a logistic regression model, evaluating it using accuracy, precision, recall, F1-score, and ROC-AUC, interpreting feature coefficients, and deploying the model using Streamlit with a saved pickle file.
